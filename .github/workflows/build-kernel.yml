name: Build ChromeOS ARCVM Kernel with SukiSU Ultra + SUSFS

on:
  workflow_dispatch:
    inputs:
      KERNEL_BRANCH:
        description: 'Select kernel branch'
        required: true
        default: 'chromeos-5.10-arcvm'
        type: choice
        options:
          - 'chromeos-5.10-arcvm'
          - 'chromeos-5.15-arcvm'
      
      ANDROID_VERSION:
        description: 'Select Android version'
        required: true
        default: 'android13'
        type: choice
        options:
          - 'android12'
          - 'android13'
          - 'android14'
          - 'android15'
      
      KERNEL_VERSION:
        description: 'Select kernel version'
        required: true
        default: '5.10'
        type: choice
        options:
          - '5.10'
          - '5.15'
          - '6.1'
          - '6.6'
      
      VFS_HOOKS:
        description: 'Enable VFS manual hooks'
        required: false
        type: boolean
        default: true
      
      KPM:
        description: 'Enable KPM (Kernel Patch Module)'
        required: false
        type: boolean
        default: true
      
      ZRAM:
        description: 'Enable ZRAM enhancement'
        required: false
        type: boolean
        default: true

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 1
          
      - name: Setup build environment
        run: |
          echo "🔄 Updating package lists..."
          sudo apt-get update
          
          echo "📦 Installing build dependencies..."
          sudo apt-get install -y build-essential python3 git curl bc bison flex libssl-dev libelf-dev ccache \
            pkg-config zip unzip zlib1g-dev libglib2.0-dev binutils-dev libunwind-dev liblzma-dev lsb-release \
            linux-headers-$(uname -r) dwarves
          
          echo "🔍 Verifying installed packages..."
          for pkg in gcc make python3 git curl bc bison flex ccache; do
            if ! command -v $pkg &> /dev/null; then
              echo "❌ Required tool $pkg not found!"
              exit 1
            fi
          done
          
          echo "✅ Build environment setup complete"
          
      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
      - name: Clone kernel source
        run: |
          echo "🔄 Preparing build environment..."
          
          # Create a clean build directory
          BUILD_DIR="build_$(date +%Y%m%d_%H%M%S)"
          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV
          
          mkdir -p "$BUILD_DIR"
          cd "$BUILD_DIR"
          
          echo "📂 Working in build directory: $BUILD_DIR"
          
          # Clone with retry mechanism
          MAX_RETRIES=3
          RETRY_COUNT=0
          
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            echo "📥 Cloning kernel source (attempt $((RETRY_COUNT + 1))/$MAX_RETRIES)..."
            if git clone --depth=1 -b ${{ github.event.inputs.KERNEL_BRANCH }} \
               https://chromium.googlesource.com/chromiumos/third_party/kernel source; then
              echo "✅ Kernel source cloned successfully"
              break
            else
              RETRY_COUNT=$((RETRY_COUNT + 1))
              if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
                echo "❌ Failed to clone kernel after $MAX_RETRIES attempts"
                exit 1
              fi
              echo "⚠️ Clone failed, retrying in 5 seconds..."
              sleep 5
            fi
          done
          
          # Verify clone success and enter directory
          if [ ! -d "source" ]; then
            echo "❌ Kernel source directory not found after clone"
            exit 1
          fi
          
          cd source
          
          # Verify branch checkout
          CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          if [ "$CURRENT_BRANCH" != "${{ github.event.inputs.KERNEL_BRANCH }}" ]; then
            echo "❌ Wrong branch checked out: $CURRENT_BRANCH (expected: ${{ github.event.inputs.KERNEL_BRANCH }})"
            exit 1
          fi
          
          # Log repository info
          echo "📋 Kernel repository information:"
          echo "Branch: $(git rev-parse --abbrev-ref HEAD)"
          echo "Commit: $(git rev-parse HEAD)"
          echo "Date: $(git log -1 --format='%cd')"
          
      - name: Clone SUSFS and Patch Repositories
        run: |
          echo "📥 Cloning SUSFS and patch repositories..."
          
          # Clone SUSFS repository with correct branch
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-${{ github.event.inputs.ANDROID_VERSION }}-${{ github.event.inputs.KERNEL_VERSION }}
          
          # Clone SukiSU patch repository with fallback methods
          if ! git clone https://github.com/ShirkNeko/SukiSU_patch.git; then
            echo "⚠️ Trying alternate clone method..."
            if ! git clone "https://github.com/ShirkNeko/SukiSU_patch.git" --depth=1; then
              echo "❌ Failed to clone SukiSU patch repository"
              exit 1
            fi
          fi
          
          if [ ! -d "susfs4ksu" ] || [ ! -d "SukiSU_patch" ]; then
            echo "❌ Failed to clone required repositories"
            exit 1
          fi
          
          echo "✅ Successfully cloned required repositories"
          
          # Verify repository contents
          echo "🔍 Verifying repository contents..."
          if [ ! -f "susfs4ksu/kernel_patches/50_add_susfs_in_gki-${{ github.event.inputs.ANDROID_VERSION }}-${{ github.event.inputs.KERNEL_VERSION }}.patch" ]; then
            echo "❌ SUSFS patch file not found!"
            exit 1
          fi
          
          if [ ! -f "SukiSU_patch/69_hide_stuff.patch" ]; then
            echo "❌ Hide Stuff patch file not found!"
            exit 1
          fi
          
          echo "✅ Repository contents verified"

      # Apply SUSFS Patches
      - name: Apply SUSFS Patches to ChromeOS Kernel
        run: |
          cd $BUILD_DIR/source
          echo "🔨 Preparing SUSFS patches..."
          
          # Create backup of original files
          echo "📦 Creating backup of original files..."
          if [ -d "fs" ]; then
            tar czf ../fs_backup.tar.gz fs/
          fi
          if [ -d "include/linux" ]; then
            tar czf ../linux_headers_backup.tar.gz include/linux/
          fi
          
          # Copy SUSFS files
          echo "📝 Copying SUSFS files..."
          PATCH_FILE="../susfs4ksu/kernel_patches/50_add_susfs_in_gki-${{ github.event.inputs.ANDROID_VERSION }}-${{ github.event.inputs.KERNEL_VERSION }}.patch"
          cp "$PATCH_FILE" ./
          cp -r ../susfs4ksu/kernel_patches/fs/* ./fs/
          cp -r ../susfs4ksu/kernel_patches/include/linux/* ./include/linux/
          
          # Apply SUSFS patch with detailed logging
          echo "🔧 Applying SUSFS patch..."
          if ! patch -p1 --dry-run < "$(basename "$PATCH_FILE")" > patch_test.log 2>&1; then
            echo "⚠️ Patch dry-run showed conflicts:"
            cat patch_test.log
            echo "Attempting to apply patch with fuzz factor..."
            if ! patch -p1 -F 3 < "$(basename "$PATCH_FILE")" > patch_apply.log 2>&1; then
              echo "❌ Failed to apply SUSFS patch:"
              cat patch_apply.log
              # Restore backups
              tar xzf ../fs_backup.tar.gz
              tar xzf ../linux_headers_backup.tar.gz
              exit 1
            fi
          else
            patch -p1 < "$(basename "$PATCH_FILE")"
          fi
          echo "✅ SUSFS patches applied successfully"

      # Apply Hide Stuff Patches
      - name: Apply Hide Stuff Patches
        run: |
          cd $BUILD_DIR/source
          echo "🔨 Applying Hide Stuff patches..."
          
          # Create backup
          git add . && git commit -m "Save state before Hide Stuff patch"
          
          # Apply patch with detailed logging
          cp ../SukiSU_patch/69_hide_stuff.patch ./
          if ! patch -p1 --dry-run -F 3 < 69_hide_stuff.patch > hide_stuff_test.log 2>&1; then
            echo "⚠️ Hide Stuff patch dry-run showed conflicts:"
            cat hide_stuff_test.log
            echo "Attempting to apply patch with force..."
            if ! patch -p1 -F 3 --force < 69_hide_stuff.patch > hide_stuff_apply.log 2>&1; then
              echo "❌ Failed to apply Hide Stuff patch:"
              cat hide_stuff_apply.log
              git reset --hard HEAD
              exit 1
            fi
          else
            patch -p1 -F 3 < 69_hide_stuff.patch
          fi
          echo "✅ Hide Stuff patches applied successfully"
          
          # Verify patch application
          echo "🔍 Verifying patches..."
          if ! git diff --check; then
            echo "⚠️ Warning: Patch verification found whitespace errors"
          fi
          echo "✅ Hide Stuff patches applied"

      # Apply VFS Hooks if enabled
      - name: Apply VFS Hooks
        if: ${{ github.event.inputs.VFS_HOOKS == 'true' }}
        run: |
          cd $BUILD_DIR/source
          echo "🔧 Preparing VFS hooks..."
          
          # Verify hook patch exists
          if [ ! -f "../SukiSU_patch/hooks/syscall_hooks.patch" ]; then
            echo "❌ VFS hooks patch not found!"
            exit 1
          fi
          
          # Create backup
          git add . && git commit -m "Save state before VFS hooks"
          
          # Copy and verify patch
          cp ../SukiSU_patch/hooks/syscall_hooks.patch ./
          
          echo "🔨 Applying VFS hooks..."
          if ! patch -p1 --dry-run -F 3 < syscall_hooks.patch > vfs_hooks_test.log 2>&1; then
            echo "⚠️ VFS hooks patch dry-run showed conflicts:"
            cat vfs_hooks_test.log
            echo "Attempting to apply patch with force..."
            if ! patch -p1 -F 3 --force < syscall_hooks.patch > vfs_hooks_apply.log 2>&1; then
              echo "❌ Failed to apply VFS hooks patch:"
              cat vfs_hooks_apply.log
              git reset --hard HEAD
              exit 1
            fi
          else
            patch -p1 -F 3 < syscall_hooks.patch
          fi
          
          # Verify patch application
          if ! git diff --check; then
            echo "⚠️ Warning: VFS hooks patch verification found whitespace errors"
          fi
          echo "✅ VFS hooks applied successfully"

      # Apply ZRAM enhancements if enabled
      - name: Apply ZRAM Enhancements
        if: ${{ github.event.inputs.ZRAM == 'true' }}
        run: |
          cd kernel
          echo "🔧 Preparing ZRAM enhancements..."
          
          # Verify ZRAM files exist
          for dir in "include/linux" "lib" "crypto"; do
            if [ ! -d "../SukiSU_patch/other/zram/lz4k/$dir" ]; then
              echo "❌ ZRAM directory not found: $dir"
              exit 1
            fi
          done
          
          # Create backup of target directories
          for dir in "include/linux" "lib" "crypto"; do
            if [ -d "$dir" ]; then
              tar czf "../${dir//\//_}_backup.tar.gz" "$dir/"
            fi
          done
          
          echo "📦 Copying ZRAM files..."
          # Copy ZRAM related files with error checking
          for src in "../SukiSU_patch/other/zram/lz4k/include/linux" "../SukiSU_patch/other/zram/lz4k/lib" "../SukiSU_patch/other/zram/lz4k/crypto"; do
            dest="${src#../SukiSU_patch/other/zram/lz4k/}"
            if ! cp -r "$src"/* "./$dest/"; then
              echo "❌ Failed to copy ZRAM files from $src"
              # Restore backups
              for backup in ../*_backup.tar.gz; do
                dir="${backup##*/}_backup.tar.gz"
                dir="${dir%_backup.tar.gz}"
                dir="${dir//_//}"
                tar xzf "$backup"
              done
              exit 1
            fi
          done
          
          # Copy LZ4K Oplus with verification
          if [ -d "../SukiSU_patch/other/zram/lz4k_oplus" ]; then
            cp -r ../SukiSU_patch/other/zram/lz4k_oplus ./lib/
          else
            echo "⚠️ LZ4K Oplus directory not found, skipping..."
          fi
          
          # Apply LZ4KD patch if available
          ZRAM_PATCH="../SukiSU_patch/other/zram/zram_patch/${{ github.event.inputs.KERNEL_VERSION }}/lz4kd.patch"
          if [ -f "$ZRAM_PATCH" ]; then
            echo "🔨 Applying LZ4KD patch..."
            cp "$ZRAM_PATCH" ./
            
            if ! patch -p1 --dry-run -F 3 < lz4kd.patch > zram_test.log 2>&1; then
              echo "⚠️ LZ4KD patch dry-run showed conflicts:"
              cat zram_test.log
              echo "Attempting to apply patch with force..."
              if ! patch -p1 -F 3 --force < lz4kd.patch > zram_apply.log 2>&1; then
                echo "❌ Failed to apply LZ4KD patch:"
                cat zram_apply.log
                # Restore from backups
                for backup in ../*_backup.tar.gz; do
                  dir="${backup##*/}_backup.tar.gz"
                  dir="${dir%_backup.tar.gz}"
                  dir="${dir//_//}"
                  tar xzf "$backup"
                done
                exit 1
              fi
            else
              patch -p1 -F 3 < lz4kd.patch
            fi
          else
            echo "⚠️ LZ4KD patch not found for kernel version ${{ github.event.inputs.KERNEL_VERSION }}"
          fi
          
          echo "✅ ZRAM enhancements applied successfully"
          
      - name: Configure kernel
        run: |
          cd $BUILD_DIR/source
          echo "🔧 Configuring kernel build environment..."
          
          # Create arch/x86/configs directory if it doesn't exist
          mkdir -p arch/x86/configs
          
          # Set up ccache for faster builds
          export CCACHE_DIR="${GITHUB_WORKSPACE}/.ccache"
          mkdir -p "${CCACHE_DIR}"
          echo "max_size = 5G" > "${CCACHE_DIR}/ccache.conf"
          
          # Create advanced compiler wrapper script
          echo "📝 Creating compiler wrapper..."
          cat > compiler-wrapper.sh << 'EOL'
          #!/bin/bash
          
          # Required compiler versions
          REQUIRED_GCC_VERSION="13.2.0"
          REQUIRED_CLANG_VERSION="17.0.0"
          MIN_GCC_VERSION="11.0.0"
          MIN_CLANG_VERSION="16.0.0"
          
          # Function to compare version numbers
          version_compare() {
              if [[ "$1" == "$2" ]]; then
                  echo "equal"
                  return
              fi
              local IFS=.
              local i ver1=($1) ver2=($2)
              for ((i=0; i<${#ver1[@]} || i<${#ver2[@]}; i++)); do
                  if ((10#${ver1[i]:-0} > 10#${ver2[i]:-0})); then
                      echo "greater"
                      return
                  elif ((10#${ver1[i]:-0} < 10#${ver2[i]:-0})); then
                      echo "less"
                      return
                  fi
              done
          }
          
          # Function to detect and validate compiler
          detect_compiler() {
              local compiler_type="unknown"
              local version=""
              
              # Check for GCC
              if command -v gcc >/dev/null 2>&1; then
                  version=$(gcc -dumpversion)
                  compiler_type="gcc"
                  if [[ $(version_compare "$version" "$MIN_GCC_VERSION") == "less" ]]; then
                      echo "❌ Error: GCC version $version is too old. Minimum required: $MIN_GCC_VERSION" >&2
                      exit 1
                  fi
                  if [[ $(version_compare "$version" "$REQUIRED_GCC_VERSION") != "equal" ]]; then
                      echo "⚠️ Warning: Using GCC $version instead of recommended $REQUIRED_GCC_VERSION" >&2
                  fi
              fi
              
              # Check for Clang
              if command -v clang >/dev/null 2>&1; then
                  local clang_version=$(clang --version | grep -oP "version \K[0-9]+\.[0-9]+\.[0-9]+")
                  if [[ -n "$clang_version" ]]; then
                      if [[ "$compiler_type" == "unknown" ]] || 
                         [[ $(version_compare "$clang_version" "$MIN_CLANG_VERSION") != "less" ]]; then
                          version=$clang_version
                          compiler_type="clang"
                          if [[ $(version_compare "$version" "$REQUIRED_CLANG_VERSION") != "equal" ]]; then
                              echo "⚠️ Warning: Using Clang $version instead of recommended $REQUIRED_CLANG_VERSION" >&2
                          fi
                      fi
                  fi
              fi
              
              if [[ "$compiler_type" == "unknown" ]]; then
                  echo "❌ Error: No suitable compiler found. Please install GCC $REQUIRED_GCC_VERSION or Clang $REQUIRED_CLANG_VERSION" >&2
                  exit 1
              fi
              
              echo "$compiler_type:$version"
          }
          
          # Default compiler flags for warning suppression
          SUPPRESS_WARNINGS=(
              -Wno-error=cast-function-type
              -Wno-error=format-truncation
              -Wno-error=format
              -Wno-error=missing-prototypes
              -Wno-error=unused-but-set-variable
              -Wno-error=unused-const-variable
          )
          
          # ChromeOS ARCVM specific flags
          ARCVM_FLAGS=(
              -mindirect-branch=thunk-extern
              -mindirect-branch-register
              -fno-jump-tables
              -fno-delete-null-pointer-checks
          )
          
          # Detect and validate compiler
          COMPILER_INFO=$(detect_compiler)
          COMPILER_TYPE=${COMPILER_INFO%%:*}
          COMPILER_VERSION=${COMPILER_INFO#*:}
          
          # Set up compiler command
          if command -v ccache >/dev/null 2>&1; then
              COMPILER="ccache"
          else
              COMPILER=""
          fi
          
          case $COMPILER_TYPE in
              "gcc")
                  COMPILER="$COMPILER gcc"
                  # GCC specific flags
                  EXTRA_FLAGS=(
                      -fno-strict-aliasing
                      -fno-common
                  )
                  ;;
              "clang")
                  COMPILER="$COMPILER clang"
                  # Clang specific flags
                  EXTRA_FLAGS=(
                      -Wno-error=unused-command-line-argument
                      -Wno-error=implicit-function-declaration
                  )
                  ;;
          esac
          
          # Log compiler configuration
          echo "🔧 Using compiler: $COMPILER_TYPE $COMPILER_VERSION" >&2
          echo "📍 Compiler path: $(which ${COMPILER##* })" >&2
          
          # Execute compiler with all flags
          exec $COMPILER "${SUPPRESS_WARNINGS[@]}" "${ARCVM_FLAGS[@]}" "${EXTRA_FLAGS[@]}" "$@"
          EOL
          chmod +x compiler-wrapper.sh
          
          # Verify wrapper script
          echo "🔍 Verifying compiler wrapper..."
          if ! ./compiler-wrapper.sh -v >/dev/null 2>&1; then
            echo "❌ Compiler wrapper verification failed!"
            exit 1
          fi
          echo "✅ Compiler wrapper created and verified"
          
          # Configure environment variables
          export PATH="$PWD:$PATH"
          export KBUILD_BUILD_USER="KaliRootSuperUser"
          export KBUILD_BUILD_HOST="github-runner"
          export KBUILD_BUILD_VERSION="1"
          export KBUILD_BUILD_TIMESTAMP="$(date -u)
          
          # Set compiler wrapper
          export HOSTCC="./compiler-wrapper.sh"
          export CC="./compiler-wrapper.sh"
          
          # Create custom config
          cat > arch/x86/configs/chromiumos-x86_64_defconfig << 'EOL'
          CONFIG_X86_64=y
          CONFIG_64BIT=y
          CONFIG_SMP=y
          CONFIG_MODULES=y
          CONFIG_MODULE_UNLOAD=y
          CONFIG_MODVERSIONS=y
          CONFIG_PARTITION_ADVANCED=y
          CONFIG_EFI=y
          CONFIG_EFI_STUB=y
          CONFIG_FB=y
          CONFIG_FRAMEBUFFER_CONSOLE=y
          CONFIG_MAGIC_SYSRQ=y
          CONFIG_MAGIC_SYSRQ_DEFAULT_ENABLE=0x1
          CONFIG_MAGIC_SYSRQ_SERIAL=y
          CONFIG_EARLY_PRINTK=y
          CONFIG_RANDOMIZE_BASE=y
          CONFIG_ZBOOT_ROM_TEXT=0x0
          CONFIG_ZBOOT_ROM_BSS=0x0
          CONFIG_PROC_KCORE=y
          CONFIG_BLK_DEV_INITRD=y
          CONFIG_KALLSYMS_ALL=y
          CONFIG_WERROR=n
          CONFIG_VDSO=y
          CONFIG_GENERIC_TIME_VSYSCALL=y
          CONFIG_COMPAT_VDSO=y
          CONFIG_SYSVIPC=y
          CONFIG_POSIX_MQUEUE=y
          CONFIG_IKCONFIG=y
          CONFIG_IKCONFIG_PROC=y
          CONFIG_MEMCG=y
          CONFIG_BLK_CGROUP=y
          CONFIG_CGROUP_SCHED=y
          CONFIG_NAMESPACES=y
          CONFIG_SECURITY=y
          CONFIG_SECURITY_NETWORK=y
          CONFIG_HARDENED_USERCOPY=y
          CONFIG_STACKPROTECTOR_STRONG=y
          CONFIG_MODULES_TREE_LOOKUP=y
          EOL
          
          # Verify kernel/sched/rt.c exists and is writable
          if [ ! -w "kernel/sched/rt.c" ]; then
            echo "⚠️ Warning: kernel/sched/rt.c is not writable or doesn't exist"
            find . -name "rt.c" -type f
          fi
          
          # Create arch/x86/configs directory if it doesn't exist
          mkdir -p arch/x86/configs
          
          # Create base defconfig file
          cat > arch/x86/configs/chromiumos-x86_64_defconfig << 'EOL'
          CONFIG_X86_64=y
          CONFIG_64BIT=y
          CONFIG_SMP=y
          CONFIG_MODULES=y
          CONFIG_MODULE_UNLOAD=y
          CONFIG_MODVERSIONS=y
          CONFIG_PARTITION_ADVANCED=y
          CONFIG_EFI=y
          CONFIG_EFI_STUB=y
          CONFIG_FB=y
          CONFIG_FRAMEBUFFER_CONSOLE=y
          CONFIG_MAGIC_SYSRQ=y
          CONFIG_MAGIC_SYSRQ_DEFAULT_ENABLE=0x1
          CONFIG_MAGIC_SYSRQ_SERIAL=y
          CONFIG_EARLY_PRINTK=y
          CONFIG_RANDOMIZE_BASE=y
          CONFIG_ZBOOT_ROM_TEXT=0x0
          CONFIG_ZBOOT_ROM_BSS=0x0
          CONFIG_PROC_KCORE=y
          CONFIG_BLK_DEV_INITRD=y
          CONFIG_KALLSYMS_ALL=y
          CONFIG_LOCALVERSION="-SukiSU-Ultra"
          CONFIG_LOCALVERSION_AUTO=n
          CONFIG_SYSVIPC=y
          CONFIG_POSIX_MQUEUE=y
          CONFIG_IKCONFIG=y
          CONFIG_IKCONFIG_PROC=y
          CONFIG_MEMCG=y
          CONFIG_BLK_CGROUP=y
          CONFIG_CGROUP_SCHED=y
          CONFIG_NAMESPACES=y
          CONFIG_SECURITY=y
          CONFIG_SECURITY_NETWORK=y
          CONFIG_HARDENED_USERCOPY=y
          CONFIG_STACKPROTECTOR_STRONG=y
          CONFIG_MODULES_TREE_LOOKUP=y
          EOL
          
          # Apply base configuration
          make ARCH=x86_64 chromiumos-x86_64_defconfig
          
          # Configure additional kernel options
          cat >> .config << 'EOL'
          CONFIG_KSU=y
          CONFIG_KPM=y
          CONFIG_LOCALVERSION="-SukiSU-Ultra"
          CONFIG_LOCALVERSION_AUTO=n
          EOL
          
          # Enable VFS hooks if requested
          if [[ "${{ github.event.inputs.VFS_HOOKS }}" == "true" ]]; then
            echo "CONFIG_KSU_MANUAL_HOOK=y" >> .config
          fi
          
          # Update configuration
          make ARCH=x86_64 olddefconfig
          
      - name: Build kernel
        run: |
          cd kernel
          echo "Building kernel with $(nproc) cores..."
          
          # Fix function prototype in vgetcpu.c
          echo 'long __vdso_getcpu(unsigned *cpu, unsigned *node, struct getcpu_cache *unused);' > arch/x86/entry/vdso/vgetcpu.h
          sed -i '1i#include "vgetcpu.h"' arch/x86/entry/vdso/vgetcpu.c
          
          # Fix function cast in sock.h
          sed -i 's/android_dst_ops_negative_advice_new_t/void (*)(struct sock *, struct dst_entry *)/' include/net/sock.h
          
          # Set additional compiler flags to handle warnings
          export KCFLAGS="-Wno-error=cast-function-type -Wno-error=format-truncation -Wno-error=format -Wno-error=missing-prototypes"
          export KCPPFLAGS="-P"
          
          # First try with standard options
          make ARCH=x86_64 CROSS_COMPILE="" -j$(nproc) all W=1 \
            KCFLAGS="$KCFLAGS" KCPPFLAGS="$KCPPFLAGS" || \
          # If failed, try with additional debug options
          make ARCH=x86_64 CROSS_COMPILE="" -j$(nproc) V=1 W=1 \
            KCFLAGS="$KCFLAGS" KCPPFLAGS="$KCPPFLAGS"
          
          # Build modules and bzImage
          make ARCH=x86_64 CROSS_COMPILE="" -j$(nproc) bzImage modules \
            KCFLAGS="$KCFLAGS" KCPPFLAGS="$KCPPFLAGS"
          
          if [ ! -f "arch/x86/boot/bzImage" ]; then
            echo "❌ Kernel build failed - bzImage not found"
            echo "Dumping last 100 lines of build log..."
            tail -n 100 .build.log
            exit 1
          fi
          
          echo "✅ Kernel build completed successfully"
          
      - name: Package kernel and source tree
        run: |
          echo "📦 Creating kernel packages..."
          
          # Create compiled kernel package
          echo "Creating compiled kernel package..."
          mkdir -p dist/kernel
          cp kernel/arch/x86/boot/bzImage dist/kernel/
          cp kernel/System.map dist/kernel/
          cp -r kernel/include dist/kernel/
          
          cd dist
          zip -r ChromeOS-ARCVM-x86_64-SukiSU-Ultra-Kernel.zip kernel/
          echo "✅ Compiled kernel package prepared"
          
          # Create source tree package
          echo "Creating source tree package..."
          cd ..
          echo "Preparing source tree for packaging..."
          
          # Clean build artifacts before packaging source
          cd kernel
          make clean
          
          # Create source package directory
          cd ..
          mkdir -p dist/source_tree
          
          # Copy the complete modified kernel source
          cp -r kernel/* dist/source_tree/
          
          # Create source tree archive
          cd dist
          tar -czf ARCVM-x86_64-SUKISU-ULTRA-SUSFS-ROOTED-KERNEL-SOURCE.tar.gz source_tree/
          echo "✅ Source tree package prepared"
          
      - name: Upload compiled kernel package
        uses: actions/upload-artifact@v4
        with:
          name: compiled-kernel
          path: dist/ChromeOS-ARCVM-x86_64-SukiSU-Ultra-Kernel.zip
          compression-level: 9
          retention-days: 90
          
      - name: Upload source tree package
        uses: actions/upload-artifact@v4
        with:
          name: kernel-source-tree
          path: dist/ARCVM-x86_64-SUKISU-ULTRA-SUSFS-ROOTED-KERNEL-SOURCE.tar.gz
          compression-level: 9
          retention-days: 90
